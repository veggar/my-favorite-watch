# My Favorite Watch — 구글 시트 기반 영상 작품 기록 관리 앱 기능 정의서

## 1. 목적

사용자가 Google 계정으로 로그인한 후, 본인의 Google Sheet를 연결하여
영화, 다큐멘터리, 애니메이션, 드라마 등 영상 작품의 관람 기록을 모바일 웹앱에서
조회, 정렬, 등록, 수정, 삭제할 수 있도록 한다.

제목 등록 시 외부 데이터베이스(TMDb 등)를 통해 해당 작품의 공식 링크를 자동으로 연결하고,
이를 제목에 하이퍼링크로 표시하여 빠른 작품 정보 접근을 가능하게 한다.

---

## 2. 범위

### 2.1 포함 범위

- Google 로그인
- Google Sheet 연결
- 목록 조회
- 검색
- 정렬 및 필터
- 등록 (작품 링크 자동 연결 포함)
- 수정
- 삭제
- 설정 관리

### 2.2 제외 범위

- 다중 사용자 협업 기능
- 별도 관리자 페이지
- 외부 공유 기능
- 소셜 기능 (팔로우, 댓글 등)

---

## 3. 데이터 모델

### 3.1 기본 컬럼

- id
- title
- titleLink
- genre
- category
- watched
- rating
- officialRating
- watchedAt
- registeredAt
- updatedAt
- review
- synopsis

### 3.2 의미

- id: 고유 식별자 (UUID 또는 타임스탬프 기반)
- title: 작품 제목
- titleLink: 작품 공식 또는 비공식 외부 링크 URL
- genre: 장르 (예: 액션, 로맨스, SF, 공포, 애니메이션 등)
- category: 작품 구분 (영화 / 드라마 / 다큐 / 애니 / 기타)
- watched: 관람 여부 (true / false)
- rating: 개인 평점 (0.0 ~ 5.0, 0.5 단위)
- officialRating: 공식 평점 (TMDb 등 외부 소스에서 자동 수집, 소수점 1자리)
- watchedAt: 관람 날짜 (관람한 경우에만 입력)
- registeredAt: 등록 일시 (자동 입력)
- updatedAt: 수정 일시 (자동 갱신)
- review: 간단 후기 (자유 텍스트)
- synopsis: 간략한 내용 요약 (자유 텍스트)

### 3.3 시트 헤더 예시

id | title | titleLink | genre | category | watched | rating | officialRating | watchedAt | registeredAt | updatedAt | review | synopsis

### 3.4 category 허용값

- 영화
- 드라마
- 다큐
- 애니
- 기타

---

## 4. 인증 및 권한

### 4.1 로그인 방식

- Google OAuth 로그인 사용

### 4.2 로그인 성공 조건

- 사용자가 Google 계정 인증을 완료함
- 앱이 기본 사용자 식별 정보를 획득함

### 4.3 시트 접근 권한

- 사용자가 지정한 Google Sheet에 대해 읽기/쓰기 가능해야 함

### 4.4 설정 완료 조건

- Google 로그인 완료
- 연결 대상 Google Sheet 지정 완료

---

## 5. 화면 구성

- 로그인 화면
- 시트 연결 화면
- 목록 화면
- 상세/등록/수정 화면 (인라인 또는 모달)
- 설정 화면

등록 및 수정은 목록 화면 내부 인라인 폼 또는 슬라이드 업 모달로 처리한다.

---

## 6. 목록 화면 기능

### 6.1 기본 구조

- 상단 앱명(My Favorite Watch) 표시
- 로그인한 계정 이름 표시
- 설정 버튼 표시
- 검색 / 필터 / 정렬 / 새로 등록 버튼을 같은 행에 배치
- 작품별 카드 목록 표시

### 6.2 작품 카드 규칙

- 각 작품은 독립된 카드로 표시
- 카드는 가로보다 세로가 긴 모바일 화면에 맞는 세로형 레이아웃을 갖는다

**카드 레이아웃 구조:**

- **1행 (주요 정보)**: 제목(titleLink가 있으면 하이퍼링크로 표시), category 배지, genre 태그, 나의 평점 / 공식 평점을 같은 행에 표시
- **2행 (부수 정보)**: 관람 날짜, 등록 날짜를 작고 흐린 텍스트로 표시

**관람 여부 UI:**

- 카드 내에 관람 여부를 직접 전환할 수 있는 토글 또는 버튼 UI 제공
  - 예: "✓ 관람 완료" / "♥ 보고 싶어요" 버튼 형태
- 관람 여부에 따라 카드 배경색 또는 테두리 색상으로 상태 구분
  - 관람 완료: 차분한 색조 (예: 연한 청록 계열)
  - 보고 싶어요: 따뜻한 색조 (예: 연한 주황/노랑 계열)
- 카드 내에서 관람 여부를 전환하면 즉시 Google Sheet에 반영

### 6.3 카드 펼침/접기

- 카드를 클릭하면 상세 정보(후기, 내용 요약) 펼침
- 펼침 상태에서 수정/삭제 버튼 노출

---

## 7. 검색 기능

### 7.1 검색 대상

- title
- genre
- review
- synopsis

### 7.2 검색 방식

- 부분 일치 검색
- 대소문자 구분 없음
- 기본 검색 대상은 title
- 전체 검색(search_scope=all) 옵션 제공 시 모든 필드를 포함
- 검색어 입력 시 자동으로 검색 실행 (엔터 불필요)

### 7.3 필터 기능

- category 필터: 전체 / 영화 / 드라마 / 다큐 / 애니 / 기타
- 관람 여부 필터: 전체 / 관람 완료 / 보고 싶어요

---

## 8. 정렬 기능

### 8.1 지원 정렬 기준

- 제목 오름차순
- 제목 내림차순
- 최근 등록일순
- 최근 수정일순
- 관람일 최신순
- 평점 높은순
- 평점 낮은순

### 8.2 기본 정렬

- 기본값: 최근 등록일순

---

## 9. 작품 링크 자동 연결 기능

### 9.1 동작 시점

- 제목이 등록될 때 (신규 등록 저장 시)
- 사용자가 수동으로 링크 갱신 요청 시

### 9.2 링크 검색 우선순위

1. TMDb (The Movie Database) 공식 작품 페이지
2. IMDb 공식 작품 페이지
3. 나무위키 또는 위키백과 해당 작품 문서 (비공식)
4. 그 외 신뢰할 수 있는 작품 소개 페이지

### 9.3 링크 저장 방식

- 검색된 URL을 `titleLink` 필드에 자동 저장
- 링크가 없을 경우 빈 값으로 저장
- 링크 연결 실패 시 사용자에게 알림 없이 무시 (선택 사항: 실패 알림 표시)

### 9.4 공식 평점 수집

- titleLink 검색과 함께 TMDb 등 외부 소스에서 공식 평점(`officialRating`)을 자동으로 수집
- 수집 실패 시 빈 값으로 저장하며 사용자에게 별도 알림 없이 무시
- 사용자가 수동으로 값을 입력하거나 수정할 수 있어야 함

### 9.5 수동 링크 입력

- 사용자가 직접 URL을 입력하여 `titleLink`를 설정할 수 있어야 함
- 수동 입력 URL은 자동 검색보다 우선 적용

---

## 10. 등록 기능

### 10.1 진입 방식

- 목록 화면 상단의 새로 등록 버튼 클릭

### 10.2 UI 방식

- 목록 화면 내부 인라인 폼 또는 슬라이드 업 모달로 표시

### 10.3 입력 항목

| 항목 | 필수 여부 | 비고 |
|------|---------|------|
| title | 필수 | 작품 제목 |
| category | 필수 | 영화/드라마/다큐/애니/기타 |
| genre | 선택 | 자유 입력 또는 다중 선택 |
| watched | 필수 | 관람 여부 토글 (기본값: false) |
| rating | 선택 | 0.0~5.0, 0.5 단위 |
| watchedAt | 조건부 | watched=true인 경우 입력 가능 |
| review | 선택 | 간단 후기 |
| synopsis | 선택 | 간략한 내용 요약 |
| titleLink | 선택 | 수동 URL 입력 (자동 연결 우선) |

### 10.4 저장 시 자동 처리

- id 자동 생성
- registeredAt 자동 입력
- updatedAt 자동 입력
- titleLink 자동 검색 및 설정 (수동 입력이 없는 경우)

### 10.5 저장 성공 조건

- 필수 입력값 검증 통과
- Google Sheet 쓰기 성공

### 10.6 등록 후 동작

- 목록 재조회 또는 로컬 상태 갱신
- 신규 카드 즉시 반영
- 등록/수정/삭제 처리 중 로딩 레이어 표시 (중복 동작 방지)

---

## 11. 수정 기능

### 11.1 진입 방식

- 작품 카드 펼침 후 수정 버튼 클릭

### 11.2 UI 방식

- 해당 카드를 인라인 편집 상태로 전환

### 11.3 수정 가능 항목

- title, category, genre, watched, rating, watchedAt, review, synopsis, titleLink

### 11.4 수정 시 자동 처리

- updatedAt 자동 갱신
- title 변경 시 titleLink 재검색 여부를 사용자에게 확인

### 11.5 수정 후 동작

- 저장 성공 시 조회 모드 복귀
- 목록 데이터 즉시 갱신

---

## 12. 삭제 기능

### 12.1 삭제 단위

- 작품 단위 삭제

### 12.2 진입 위치

- 작품 카드 펼침 상태에서 삭제 버튼 클릭

### 12.3 삭제 처리 방식

- 삭제 시 해당 작품 정보를 원본 시트에서 제거하고 별도 `삭제` 워크시트로 이관
- 삭제 이관 시 `삭제일시` 컬럼을 함께 기록

### 12.4 삭제 후 동작

- 해당 카드 즉시 제거
- 삭제 기록은 `삭제` 시트에 보관 (복구 또는 감사 목적)

---

## 13. 시트 연결 기능

### 13.1 입력 방식

- 사용자 입력 기반 Google Sheet URL 지정
- 기존 데이터가 있는 다른 Google Sheet의 URL을 통한 마이그레이션 지원
- CSV 또는 엑셀(.xlsx) 파일을 통한 일괄 업로드 기능 제공

### 13.2 연결 검증

- URL 형식 검증
- 시트 접근 가능 여부 확인
- 워크시트 존재 여부 확인

### 13.3 연결 성공 후 저장 정보

- sheetId
- worksheetName 또는 worksheetId
- 사용자 계정 기준 연결 상태
- 앱은 `My Favorite Watch`라는 이름의 워크시트를 데이터베이스로 사용하며, 해당 워크시트가 없으면 자동 생성

---

## 14. 설정 기능

### 14.1 표시 항목

- 로그인 계정 정보
- 연결된 시트 정보
- 기본 정렬값
- 기본 필터값
- 시트 다시 연결
- 로그아웃

### 14.2 기능 항목

- 시트 변경
- 로그아웃
- 정렬/필터 기본값 변경

---

## 15. 유효성 검증

### 15.1 등록/수정 필수값

- title
- category
- watched

### 15.2 허용 정책

- genre, rating, watchedAt, review, synopsis는 비필수
- watched=false인 경우 watchedAt 입력 불가 (또는 무시)
- rating은 0.0~5.0 범위, 0.5 단위로 제한
- 데이터 저장 워크시트는 `My Favorite Watch` 워크시트로 제한

### 15.3 날짜 처리

- 날짜는 ISO 문자열 또는 표준 datetime 문자열 저장 권장
- UI에서는 축약 형식으로 표시 (예: 2026.04.10)

---

## 16. 상태 관리

### 16.1 주요 상태

- 로그인 상태
- 시트 연결 상태
- 목록 데이터
- 검색어
- 검색 범위 (title 전용 / 전체)
- 정렬 기준
- 필터 조건 (category, watched)
- 등록 폼 열림 여부
- 수정 중인 작품 식별자
- 펼침 상태 작품 식별자 목록

### 16.2 식별 방식

- 각 작품은 `id` 컬럼을 통해 고유하게 식별
- id는 등록 시 자동 생성 (UUID 권장)

---

## 17. 에러 처리

### 17.1 로그인 실패

- 재시도 안내 표시

### 17.2 시트 연결 실패

- URL 오류
- 권한 없음
- 시트 없음
- 네트워크 오류

### 17.3 조회 실패

- 목록 로드 실패 메시지 표시
- 재시도 기능 제공

### 17.4 저장 실패

- 등록/수정/삭제 실패 메시지 표시
- 실패 시 기존 상태 유지

### 17.5 작품 링크 검색 실패

- 링크 연결 없이 작품 정보는 정상 저장
- 실패 알림은 선택 사항 (UX 정책에 따름)

---

## 18. 보안 및 주의사항

### 18.1 기본 원칙

- Google OAuth를 통한 사용자 인증만 허용
- 개인 시청 기록이므로 외부 노출 최소화

### 18.2 API 키 관리

- TMDb 등 외부 API 키는 서버 환경 변수(.env)로 관리
- 클라이언트에 직접 노출 금지

---

## 19. 권장 기술 구현 메모

### 19.1 프론트엔드

- 모바일 우선 반응형 웹
- 서버 사이드 렌더링(SSR) 사용

### 19.2 백엔드 또는 연동

- Google OAuth
- Google Sheets API
- TMDb API (작품 링크 자동 연결)
- IMDb 또는 나무위키 크롤링 (TMDb 없을 시 대체)

### 19.3 권장 추가 컬럼

운영 편의를 위해 아래 컬럼 추가 가능

- ownerEmail
- isDeleted

---

## 20. 완료 기준

다음 조건을 모두 만족하면 기능 개발 완료로 본다.

- Google 로그인 가능
- Google Sheet 연결 가능 (`My Favorite Watch` 워크시트 자동 생성)
- 목록 조회 가능
- 동적 검색 가능 (title 기본, 전체 검색 옵션 제공)
- category / 관람 여부 필터 가능
- 정렬 가능 (7가지 기준)
- 등록 가능 (제목 등록 시 작품 링크 자동 연결)
- 수정 가능 (title 변경 시 링크 재검색 여부 확인)
- 삭제 가능 (삭제 시 `삭제` 워크시트에 이관 기록)
- 작품 카드 클릭 시 후기/내용 펼침 가능
- 작품 제목에 하이퍼링크(공식 또는 비공식) 표시 가능
- 관람 여부 / 평점 / 관람일 / 후기 / 내용 요약 관리 가능
- 로그인한 계정 이름 표시 가능
